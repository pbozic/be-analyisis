import { z } from 'zod';

export const Delivery_order_sentScalarFieldEnumSchema = z.enum(['delivery_order_sent_id','order_id','accepted','location','timeline','created_at','updated_at','delivery_driver_id','driver_id']);

export default Delivery_order_sentScalarFieldEnumSchema;
