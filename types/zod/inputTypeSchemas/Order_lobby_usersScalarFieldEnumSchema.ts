import { z } from 'zod';

export const Order_lobby_usersScalarFieldEnumSchema = z.enum(['order_lobby_users_id','user_id','order_lobbies_id','limit','created_at','updated_at']);

export default Order_lobby_usersScalarFieldEnumSchema;
