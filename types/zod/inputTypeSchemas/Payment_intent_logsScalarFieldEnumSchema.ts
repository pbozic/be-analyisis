import { z } from 'zod';

export const Payment_intent_logsScalarFieldEnumSchema = z.enum(['payment_intent_logs_id', 'stripe_id', 'created_at']);

export default Payment_intent_logsScalarFieldEnumSchema;
