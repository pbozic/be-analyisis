type: object
properties:
    token_id:
        type: string
    user_id:
        type: string
    token:
        type: string
    expires_at:
        type: string
    created_at:
        type: string
    updated_at:
        type: string
    active:
        type: boolean
    users:
        $ref: ./users.yaml
required:
    - token_id
    - user_id
    - token
    - expires_at
    - created_at
    - updated_at
    - active
