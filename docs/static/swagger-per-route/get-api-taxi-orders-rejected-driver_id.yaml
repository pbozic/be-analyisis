openapi: 3.0.3
info:
  title: get /api/taxi/orders/rejected/:driver_id
  version: 1.0.0
paths:
  /api/taxi/orders/rejected/:driver_id:
    get:
      tags:
        - Taxi
      summary: Get rejected taxi orders.
      description: This fetches all rejected orders for a specific driver.
      operationId: getRejectedTaxiOrders
      responses:
        '200':
          description: >-
            Successful operation. Returns a list of rejected orders in the
            response body.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Order'
        '500':
          description: >-
            Server error. Returns error message "Error something went wrong..."
            if any exception is encountered during execution.
