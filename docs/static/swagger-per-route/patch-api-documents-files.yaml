openapi: 3.0.3
info:
  title: patch /api/documents/files
  version: 1.0.0
paths:
  /api/documents/files:
    patch:
      tags:
        - Documents
      summary: Update document's files
      description: Updates the files associated with a specific document.
      operationId: updateDocumentFiles
      parameters:
        - name: document_id
          in: path
          description: The ID of the document to update
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateFilesRequest'
        required: true
      responses:
        '200':
          description: Document files updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '400':
          description: Error updating document's files
