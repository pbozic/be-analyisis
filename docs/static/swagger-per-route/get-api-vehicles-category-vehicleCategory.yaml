openapi: 3.0.3
info:
  title: get /api/vehicles/category/:vehicleCategory
  version: 1.0.0
paths:
  /api/vehicles/category/:vehicleCategory:
    get:
      tags:
        - Vehicles
      summary: Get vehicles by category
      description: Retrieves a list of vehicles of a specific category.
      parameters:
        - name: vehicleCategory
          in: path
          description: The category of the vehicles to retrieve
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Vehicle'
        '500':
          description: Error fetching vehicles
