openapi: 3.0.3
info:
  title: post /api/business/delivery-address/add
  version: 1.0.0
paths:
  /api/business/delivery-address/add:
    post:
      tags:
        - Business
      summary: Add a delivery address to a business
      description: Adds a delivery address to a specific business.
      operationId: addDeliveryAddress
      parameters:
        - name: business_id
          in: path
          description: The ID of the business
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Address'
        required: true
      responses:
        '200':
          description: Delivery address added successfully
        '400':
          description: Error adding delivery address
