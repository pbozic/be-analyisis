openapi: 3.0.3
info:
  title: patch /api/business/business-group-name
  version: 1.0.0
paths:
  /api/business/business-group-name:
    patch:
      tags:
        - Businesses
      summary: Updates a business's group name
      description: This endpoint is used to update a business's group name.
      operationId: updateBusinessGroupName
      requestBody:
        description: The new group name for the business
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBusinessGroupNameRequest'
        required: true
      responses:
        '200':
          description: >-
            Group name updated successfully. Returns the updated business
            details.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Business'
        '400':
          description: Error updating business information.
