openapi: 3.0.3
info:
  title: patch /api/overwatch/drivers/activity/settings
  version: 1.0.0
paths:
  /api/overwatch/drivers/activity/settings:
    patch:
      tags:
        - DriverSettings
      summary: Update driver activity settings
      description: >-
        Updates existing driver activity settings or creates new ones if they
        don't exist
      operationId: updateDriverActivitySettings
      requestBody:
        content:
          application/json:
            schema:
              $ref: |
                #/components/schemas/
                  "first_offline_lockout": 30,
                  "second_offline_lockout": 120,
                  "online_timeout": 120,
                  "active": true
        required: true
      responses:
        '200':
          description: Settings updated successfully
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Invalid request data
        '500':
          description: Server error while updating settings
